import { memo } from "react";
import { Page } from "widgets/Page";

const AdminPanelPage = () => {
  return (
    <Page data-testid="AdminPanelPage">test</Page>
  );
};

export default memo(AdminPanelPage);
